#ifndef SPECIAL_CMP
#define SPECIAL_CMP

#include <stdbool.h>

bool array_eq(float* arr1, float* arr2, int size);
bool model_matrix_eq(float* model_matrix1, float* model_matrix2);

#endif /* ifndef SPECIAL_CMP */
